name: 🧠 SOI Knowledge Pump

on:
  push:
    paths:
      - 'topics.txt'
  workflow_dispatch:

jobs:
  harvest_knowledge:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v3

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Install Dependencies
      run: |
        pip install requests pypdf arxiv wikipedia

    - name: 🔥 Run The Hunter
      run: python trainer.py
      
    - name: 📊 Report Status
      if: always()
      run: |
        echo "✅ Knowledge Pump cycle completed"